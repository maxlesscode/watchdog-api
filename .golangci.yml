version: "2"

run:
  timeout: 5m
  exclude-directories:
    - data
    - vendor

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign

issues:
  exclude-use-default: true
